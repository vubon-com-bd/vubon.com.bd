/**
 * AuthMfaService — DI tokens (re-export)
 * @module auth-service/application/services/impl
 */
export {
  AUTH_MFA_REPO,
  USER_REPO,
  TOTP_SERVICE,
  RECOVERY_CODE_GENERATOR,
} from '../../tokens';
