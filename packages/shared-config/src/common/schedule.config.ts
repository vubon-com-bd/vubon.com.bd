export const scheduleConfig = {
  timezone: 'Asia/Dhaka',
  maxSchedules: 50,
  retentionDays: 90,
  maxRecurringSchedules: 20,
  timeout: 60 * 60, // 60 minutes
  frequencies: ['hourly', 'daily', 'weekly', 'monthly', 'quarterly', 'annual'],
};
