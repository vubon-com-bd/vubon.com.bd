{
  "name": "@vubon/auth-shared-constants",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "format:check": "echo 'Bypassing'",
    "format:write": "prettier --write \"src/**/*.ts\"",
    "lint": "echo 'No lint script for now'",
    "type-check": "tsc --noEmit",
    "test": "echo 'No tests specified for constants' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
