import { SEARCH_TYPE, SEARCH_SCOPE } from './search-type.constants';
import { SEARCH_SORT, SEARCH_SORT_ORDER, SEARCH_SORT_DEFAULT } from './search-sort.constants';
import { SEARCH_FILTER_TYPE, SEARCH_FILTER_RANGE, SEARCH_FILTER } from './search-filter.constants';
import { SEARCH_OPERATOR, SEARCH_BOOLEAN, SEARCH_WILDCARD } from './search-operator.constants';
import { SEARCH_MATCH_TYPE, SEARCH_FUZZINESS, SEARCH_MATCH } from './search-match.constants';
import { SEARCH_BOOST_FIELD, SEARCH_BOOST, SEARCH_BOOST_LIMIT } from './search-boost.constants';
import {
  SEARCH_ANALYTICS_METRIC,
  SEARCH_ANALYTICS_PERIOD,
  SEARCH_ANALYTICS,
} from './search-analytics.constants';
import { SEARCH_SUGGESTION_TYPE, SEARCH_SUGGESTION } from './search-suggestion.constants';
import { SEARCH_SYNONYM_TYPE, SEARCH_SYNONYM } from './search-synonym.constants';
import { SEARCH_INDEX_TYPE, SEARCH_INDEX_STATUS, SEARCH_INDEX } from './search-index.constants';
import { SEARCH_RELEVANCE_ALGORITHM, SEARCH_RELEVANCE } from './search-relevance.constants';
import { AUTOCOMPLETE_TYPE, AUTOCOMPLETE } from './autocomplete.constants';
import { FACET_TYPE, FACET_FIELD, FACET_SORT, FACET } from './facet.constants';

export const SEARCH_LIMIT = {
  MIN_QUERY_LENGTH: 2,
  MAX_QUERY_LENGTH: 200,
  DEFAULT_PAGE_SIZE: 20,
  MAX_PAGE_SIZE: 100,
  MAX_RESULTS: 10000,
  MAX_FILTERS: 50,
  MAX_FACETS: 30,
  MAX_SUGGESTIONS: 10,
  QUERY_TIMEOUT_MS: 5000,
  CACHE_TTL_SECONDS: 300,
  RETENTION_DAYS: 365,
} as const;

export const SEARCH = {
  TYPE: SEARCH_TYPE,
  SCOPE: SEARCH_SCOPE,
  LIMIT: SEARCH_LIMIT,

  SORT: {
    TYPE: SEARCH_SORT,
    ORDER: SEARCH_SORT_ORDER,
    DEFAULT: SEARCH_SORT_DEFAULT,
  },

  FILTER: {
    TYPE: SEARCH_FILTER_TYPE,
    RANGE: SEARCH_FILTER_RANGE,
    LIMIT: SEARCH_FILTER,
  },

  OPERATOR: {
    TYPE: SEARCH_OPERATOR,
    BOOLEAN: SEARCH_BOOLEAN,
    WILDCARD: SEARCH_WILDCARD,
  },

  MATCH: {
    TYPE: SEARCH_MATCH_TYPE,
    FUZZINESS: SEARCH_FUZZINESS,
    LIMIT: SEARCH_MATCH,
  },

  BOOST: {
    FIELD: SEARCH_BOOST_FIELD,
    VALUE: SEARCH_BOOST,
    LIMIT: SEARCH_BOOST_LIMIT,
  },

  ANALYTICS: {
    METRIC: SEARCH_ANALYTICS_METRIC,
    PERIOD: SEARCH_ANALYTICS_PERIOD,
    LIMIT: SEARCH_ANALYTICS,
  },

  SUGGESTION: {
    TYPE: SEARCH_SUGGESTION_TYPE,
    LIMIT: SEARCH_SUGGESTION,
  },

  SYNONYM: {
    TYPE: SEARCH_SYNONYM_TYPE,
    LIMIT: SEARCH_SYNONYM,
  },

  INDEX: {
    TYPE: SEARCH_INDEX_TYPE,
    STATUS: SEARCH_INDEX_STATUS,
    LIMIT: SEARCH_INDEX,
  },

  RELEVANCE: {
    ALGORITHM: SEARCH_RELEVANCE_ALGORITHM,
    LIMIT: SEARCH_RELEVANCE,
  },

  AUTOCOMPLETE: {
    TYPE: AUTOCOMPLETE_TYPE,
    LIMIT: AUTOCOMPLETE,
  },

  FACET: {
    TYPE: FACET_TYPE,
    FIELD: FACET_FIELD,
    SORT: FACET_SORT,
    LIMIT: FACET,
  },
} as const;

export type SearchType = typeof SEARCH;
